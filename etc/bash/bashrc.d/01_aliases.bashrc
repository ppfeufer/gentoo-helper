#!/usr/bin/env bash

# Aliases
alias ll='lsd -lh'
alias la='lsd -lAh'
alias ..='cd ..'
alias ...='cd ../..'
alias s='ssh -l root'
alias please='sudo'

# Make aliases work with sudo
alias sudo='sudo '
